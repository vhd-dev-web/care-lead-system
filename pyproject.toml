[project]
name = "care-lead-system"
version = "0.1.0"
description = "Lead system for German Pflegehilfsmittel / Pflegebox providers (forked from vhd-lead-system)."
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12",
    "httpx>=0.27",
    "openpyxl>=3.1",
    "pydantic>=2.7",
    "typer>=0.12",
]

[project.optional-dependencies]
google = [
    "google-api-python-client>=2.120",
    "google-auth>=2.28",
]

[project.scripts]
care-enrich = "lead_enrichment.cli:main"
care-lead-scrape = "care_lead_system.scraper.woocommerce_lead_finder:main"
care-lead-setup = "care_lead_system.setup_wizard:main"
care-lead-verify = "care_lead_system.scraper.lead_qualifier:main"
care-lead-system = "care_lead_system.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
